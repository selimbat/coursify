# Drizzle
# Replace with your DB credentials!
DATABASE_URL="postgres://user:password@host:port/db-name"

# Auth
# Generate with: node -e "const{createHash}=require('crypto');console.log(createHash('sha256').update('YOUR_PASSPHRASE').digest('hex'))"
PASSPHRASE_HASH=""

# JWT secret — use a long random string, e.g.: openssl rand -hex 32
JWT_SECRET=""
