{
  "name": "coursify",
  "version": "0.1.0",
  "description": "Shared shopping list app for couples - SvelteKit + PostgreSQL",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "db:migrate": "node scripts/migrate.js",
    "db:setup": "npm run db:migrate"
  },
  "dependencies": {
    "svelte": "^4.2.0",
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "@sveltejs/kit": "^2.0.0",
    "@sveltejs/vite-plugin-svelte": "^3.0.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0",
    "svelte-check": "^3.6.0"
  }
}
